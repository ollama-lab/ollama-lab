// Reexport invoke for future convenience
//
// If we want to port this frontend to backend other than Tauri, we can just update this file (theoretically)
export { invoke } from "@tauri-apps/api/tauri"
